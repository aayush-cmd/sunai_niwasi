# Sunai Niwasi

Parent repo. Apps are git submodules:

- `apps/frontend` — Niwashi_frontend
- `apps/api` — Niwasi_backend

Clone with:

```sh
git clone --recurse-submodules git@github.com:aayush-cmd/sunai_niwasi.git
```
